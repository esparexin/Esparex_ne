{
  "name": "@esparex/shared",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=22.0.0 <23"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "type-check": "npm run build -w @esparex/contracts && tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@esparex/contracts": "*",
    "@sentry/node": "^10.68.0",
    "slugify": "^1.6.9",
    "zod": "^4.4.3"
  }
}
